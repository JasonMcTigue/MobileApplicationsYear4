<UserControl
    x:Class="SavingsAccumulator.UserControls.AddTargetControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SavingsAccumulator.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>

        </Grid.RowDefinitions>

        <StackPanel Orientation="Vertical">
            <TextBlock Text="Target Name:" />
            <TextBox x:Name="TargetNameTxtBox" />
        </StackPanel>

        <StackPanel  Grid.Row="1" Orientation="Vertical">
            <TextBlock Text="Amount Saved:" />
            <TextBox x:Name="SavedAmtTxtBox" />
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Vertical">
            <TextBlock Text="Notes:" />
            <TextBox x:Name="NotesTxtBox" />
        </StackPanel>

        <Button Grid.Row="3" Content="Save" HorizontalAlignment="Right" />
        <Button Grid.Row="3" Content="Delete" HorizontalAlignment="Left"/>

    </Grid>
</UserControl>
