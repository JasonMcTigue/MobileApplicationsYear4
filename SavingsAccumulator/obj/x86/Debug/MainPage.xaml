<Page
    x:Class="SavingsAccumulator.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SavingsAccumulator"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:CC="using:SavingsAccumulator.UserControls"
    xmlns:Converter="using:SavingsAccumulator.Converters"
    mc:Ignorable="d">

    <Page.Resources>
        <Converter:BoolVisibilityConverter x:Key="BoolVisibility"/>
    </Page.Resources>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <ListView x:ConnectionId='1' x:Name="TargetListView" Grid.Row="0" ItemsSource="{Binding TargetList}"><!--binds to target list-->
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>

                </Style>
                

            </ListView.ItemContainerStyle>

            <ListView.ItemTemplate>
                <DataTemplate>

                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition/>
                            <RowDefinition/>
                            <RowDefinition/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>
                        
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                            
                        </Grid.ColumnDefinitions>

                        <!--Target text block-->
                        <TextBlock Text="Target Name: "/>
                        <TextBlock Grid.Column="1" Text="{Binding Name}"/>

                        <!--Last time it was updated-->
                        <TextBlock Margin="0,10,0,0" Grid.Row="1" Text="Amount Saved: "/>
                        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Date}"/>

                        <!--progress bar spans through out all 3 columns-->
                        <ProgressBar Margin="0,10,0,10" Grid.Row="2" Value="{Binding CurrentBalance}" Maximum="{Binding SavingTarget}" Grid.ColumnSpan="3" IsIndeterminate="False" Height="10"/>

                        <Button Grid.Row="3" Grid.Column="0" Content="Add " Command="{Binding DataContext.TransactionButtonCommand, ElementName=TargetListView}"/>
                        <Button Grid.Row="3" Grid.Column="1" Content="Minus " Command="{Binding DataContext.TransactionButtonCommand, ElementName=TargetListView}"/>
                        <Button Grid.Row="3" Grid.Column="2" Content="Edit "/>

                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>

           <!-- <ListView Grid.Row="0"/>-->

        </ListView>

        <CommandBar Grid.Row="1" ClosedDisplayMode="Minimal">
            <AppBarButton x:ConnectionId='4' Icon="Add" Label="Add Target"                           />
        </CommandBar>

        <!--Binds the trasaction page to the main page using converter-->
        <CC:AddTransactionControl x:ConnectionId='2' x:Name="TransactionControl" Grid.RowSpan="2" Visibility="{Binding ShowTransactionControl, Converter={StaticResource BoolVisibility}}"/>
        <CC:AddTargetControl x:ConnectionId='3' x:Name="TargetControl" Grid.RowSpan="2" Visibility="Collapsed"/>
    </Grid>
</Page>

